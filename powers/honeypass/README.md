# HoneyPass Power 🛂🍯
Agent Plugins 1.0 package (portable core: `plugin.json`, `skills/`, `mcp.json`; Kiro extension: `dev.kiro/`).
The MCP server entry point `honeypass-mcp` is implemented by the `honeypass-mcp-power` spec.
Install in Kiro from this repo's GitHub URL once the MCP server is built.
